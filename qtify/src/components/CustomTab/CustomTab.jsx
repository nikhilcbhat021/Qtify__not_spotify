function a11yProps(index) {
    return {
        id: `genre-tab-${index}`,
        'aria-controls': `genre-tabpanel-${index}`,
    };
}

function CustomTab({ key, iterable, children }) {

    return <>CustomTab TODO...</>
}


export default CustomTab;